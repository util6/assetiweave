# A-G01：任务一独立验收与交付

> **Status: PLANNED**。本卡不开展插件架构实现。

**Goal:** 证明成熟库已经替代旧机制，契约统一，业务与数据可用，形成下一任务的可信输入。
**Depends:** A-C02。
**Contracts:** 全部 C-*；仅在本验收卡全量读取。
**Gates:** G-FINAL，包含全部前置 gate。

## 文件与输出

- Inspect: 当前依赖表的生产使用点、所有本任务删除/修改清单、所有卡交接、当前源码和锁文件。
- Modify: 只修复明确验收缺陷；涉及新的模块设计必须单列修复卡再评审。
- Output: Issue #22 的任务验收评论；获准发布时按既有流程提交/发布，不由此卡自行部署用户机器。

## 步骤

- [ ] 检查 A00、A-F01–A-F16、A-R01–A-R12、A-C01/A-C02 全部有 VERIFIED 证据；任何缺证据的卡保持未完成。
- [ ] 对比 A00：逐依赖列生产调用方、删除的自研函数/文件、保留的领域逻辑、迁移后测试。纯安装无生产接入属于未完成。
- [ ] 检查 `resolveAppRoute/createCachedLoader`、旧 asyncCache/手写 query 轮询、旧 i18n 插值、已替代 Context 数据缓存、逐行文件 logger、ureq 调用、已替代 Condvar 追踪均无遗留生产职责。不能通过删除业务算法或测试降低计数。
- [ ] 确認 package/lock 中没有本次引入却未使用的包；没有新旧并行树或永不过期的兼容包装。保留边界脚本、Team 投影、TaskRuntime/安装策略等真实业务模块。
- [ ] 执行 `../04-verification-matrix.md` 的 G-FINAL 全套命令和桌面/跨平台场景；记录最新 revision 的结果。既有失败如果仍影响产品或验收，任务不标整体完成。
- [ ] 对涉及 locale 的真实旧库**一致快照副本**做升级演练；旧库/旧版能回退，新库另存，原始资料不作为实验对象。不要用普通文件复制替代正在 WAL 写入的数据库一致备份。
- [ ] 完成独立审查：代码正确性、业务规格、简化效果、契约稳定和退出/租户/错误回归分别有结论；阻断项为零。
- [ ] 发布任务一交接，列可复用技术接口、锁版本、测试命令、实际保留的插件接缝；给出下一任务 B00 的输入 revision。任务二仍从自己的入口开始。

## 给任务二的交接字段

`accepted_revision`、本工作区未提交内容处理结果、`pnpm-lock.yaml`/`Cargo.lock` 所在 revision、外部 contract/protocol/settings schema 版本、数据备份/回退验证、G-FINAL 测试与平台证据、仍保留的 ExtensionKernel/Conversation/ACP 业务边界。

**完成定义：** 用户可以单独采用任务一成果而不依赖任何任务二代码。只有上述所有步骤有证据时，才将 Issue 标成已交付，并允许第二目录的 B00 进入执行队列。
